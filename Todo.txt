Tasks for XBTV:

Window for current channel display - show at least number and title
prettyify the EPG (animation?)
    if moving in the epg, animate buttons
Channel names
ability to start some channels paused
sleep timer
    timer works, only need the cancel window
-Select and start any show from the EPG, even in past or future
-Pause any channel and continue watching TV on another...that channel will still be paused when you return
-This is still a media center: you can still fast-forward and rewind shows at will
-User-defined channels. Just create a smart playlist for each channel and it will automatically be added




Known Issues
When switching rapidly between channels, the video won't show
Using the EPG to switch to a different show on the current channel doesn't work